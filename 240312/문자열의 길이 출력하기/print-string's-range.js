// 변수 선언 및 입력
const fs = require("fs");
let input = fs.readFileSync(0).toString().trim();
let input2= fs.readFileSync(0).toString().trim();




// 문자열을 두 번 출력합니다.
console.log(input.length + input2.length-1);